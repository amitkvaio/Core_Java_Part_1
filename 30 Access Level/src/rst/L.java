package rst;

class L {
	public static void main(String[] args) {
		// default1.A a1 = new default1.A();
	}
}
//CTE
/*
A, access level is default so it can be used in the default package 
it is not used outside of that
but we are using A inside the RST package so compiler will not allowing us
so CTE
*/